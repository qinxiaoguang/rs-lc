use super::Solution;

impl Solution {
    // 其实就是列出状态转移即可，状态转移可以有很多种，不想写了
    pub fn is_number(s: String) -> bool {
        false
    }
}
#[cfg(test)]
mod test {
    use super::*;
    #[test]
    fn test_j020() {}
}
